# CogniTest — project overview

The **maintained** architecture and developer guide live here:

**[`docs/PROJECT-OVERVIEW.md`](docs/PROJECT-OVERVIEW.md)**

It covers the stack, request/auth flow, test catalogue (`public.tests` + `mockTests` merge), session merge logic, dual persistence (API submissions vs `finishSession`), routing, and production gaps.

For AI assistants in Cursor, see also **[`CLAUDE.md`](CLAUDE.md)** (short stack + pointers).
